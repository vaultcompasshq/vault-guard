{
  "name": "@vaultcompass/vault-guard-monorepo",
  "version": "1.0.0",
  "description": "Security and optimization layer for AI-native coding (workspace root — not published)",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r --workspace-concurrency=1 run test:coverage",
    "gen:rules-doc": "node scripts/gen-rules-doc.cjs",
    "lint": "eslint .",
    "prepublishOnly": "pnpm build",
    "release": "pnpm publish -r --access public"
  },
  "keywords": [
    "cli",
    "security",
    "ai-coding",
    "guardrails",
    "secret-scanner",
    "token-optimization",
    "pre-commit-hook"
  ],
  "author": "Vault & Compass LLC",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^30.0.0",
    "typescript": "^5.0.0",
    "eslint": "^10.2.1",
    "@typescript-eslint/eslint-plugin": "^8.59.1",
    "@typescript-eslint/parser": "^8.59.1",
    "@eslint/js": "^9.0.0",
    "jest": "^30.3.0"
  },
  "packageManager": "pnpm@9.15.9",
  "pnpm": {
    "overrides": {
      "esbuild": "^0.25.0"
    }
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
