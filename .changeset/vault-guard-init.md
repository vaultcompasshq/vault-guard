---
"@vaultcompass/vault-guard": minor
---

Add `vault-guard init` for one-command repository setup: config, CI workflow, agent guardrail files, pre-commit hook, manifest-based revert, dry-run, and conflict-safe (no-overwrite) behavior.
