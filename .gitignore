node_modules/
dist/
*.log

# Generated benchmark fixtures (contain synthetic secret-shaped strings for
# recall testing; generated at runtime by bench/generate-fixtures.cjs).
bench/fixtures/secrets/
.DS_Store
.env
.env.local
*.tgz
coverage/
.nyc_output/
tmp-test-*
tmp-test-integration/

# Local-only planning and working notes (kept on disk, not tracked).
.local/

# Maintainer records. A backlog of open work and a release dogfood checklist
# are notes to ourselves, not documentation for anyone installing this. They
# live in the private notes repository now; these lines stop them coming back.
docs/BACKLOG.md
docs/DOGFOOD.md

# Legacy paths (files now under .local/archive/); keep ignored if recreated.
marketing/
docs/PRODUCTION_PLAN.md
docs/AWESOME_LISTS.md
docs/DESIGN_PARTNERS.md
docs/SCREENCAST.md
docs/ISSUE_TRIAGE.md

# Local-only working notes (never commit).
TODO.local.md
*.local.md
docs/sessions/
docs/plans/

# Local AI configs — never commit (Vault & Compass convention).
.cursor/
.claude/
audit.md

# This is a pnpm workspace; pnpm-lock.yaml is the only lockfile. A stray
# package-lock.json from an npm invocation is dead weight that dependabot
# still scans, and one tracked since 1.0.0 produced six alerts for versions
# the real lockfile had long since patched.
package-lock.json

# The umbrella's hook manifest, under .guardrails, records absolute paths on
# whichever machine installed the hook, so it stays local. The policy file
# beside it, .guardrails.yaml, is tracked: CI reads it.
.guardrails/
